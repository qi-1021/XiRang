# 🌱 息壤 (XiRang) v4.0：愿景差距剖析与架构演进规划

> **“息壤者，言土自长息无限也。”**
> 息壤旨在打造一款面向全平台异构环境（Windows / macOS / Linux / BSD / 华为鸿蒙 PC / 嵌入式工控机）的**超轻量、单文件、强针对性、生生不息操作系统伴生自愈智能体**。

---

## 一、 愿景对标与总体现状评估 (Vision & Current State)

根据项目设计初衷，息壤定位为工业级操作系统伴生自愈智能体。通过对现有代码库（Go 核心引擎 `main.go`、构建器 `builder/`、图形界面 `builder/gui/`、C 原生引擎 `c_engine/xirang.c`、鸿蒙 `harmony_engine/ZenAgent.ets`）与文档的全面盘点，总体对标情况如下：

### 核心亮点 (已具备)
1. **零外部依赖单文件空投**：无 Python/Node/Docker 依赖，原生编译可直接在 Windows/Linux/macOS/鸿蒙 PC 上运行。
2. **人类决策主权 (Human Authority)**：`ask_human` 交互机制，核心选址与敏感操作交由人类判断。
3. **DoD 终态硬性防作弊网关 (`-verify`)**：以真实命令（如 `curl` 端点探活）为唯一准绳，拒绝幻觉。
4. **事务撤销与原子恢复 (`--rollback`)**：自动备份修改文件并支持逆序撤销。
5. **多线并发批处理 (Parallel Batch Actions)**：并发多命令/多文件操作，聚合回报。

---

## 二、 核心架构差距剖析 (Architecture Gap Analysis)

为了让息壤从“可用”迈向“工业级极致自愈”，以下领域存在需要重点攻克与改进的架构差距：

### 1. Fast-Path 秒级自愈与技能指纹匹配 (Fast-Path Cache Engine)
* **现状差距**：前期技能匹配仅依赖简单字符串 `strings.Contains` 或单一正则匹配，无法准确处理包含多错因的复合日志，容易造成误匹配或漏匹配。
* **改进方向**：引入**多关键词权重评分**与**正则模糊指纹复合匹配**机制，自动识别最匹配的自愈脚本；未来可演进为极轻量嵌入式向量/TF-IDF 指纹检索。

### 2. 安全沙箱防越界与原子回滚 (PathGuard & Rollback Stack)
* **现状差距**：
  * **路径穿越风险**：原 `PathGuard` 仅使用字符串前缀检查，未做路径规范化（`filepath.Clean` / `filepath.Rel`），可能被 `../` 等路径穿越绕过。
  * **撤销栈不完整**：原撤销仅跟踪被修改/覆盖的文件，未记录智能体过程中**新建的文件**或**创建的目录**，撤销后仍残留垃圾文件。
* **改进方向**：
  * 引入规范化绝对路径解析，彻底杜绝路径穿越；
  * 在 `RollbackOp` 恢复栈中显式标记 `OpType`（`TypeModify` 与 `TypeCreate`），撤销时自动删除新建文件，恢复绝对纯净环境。

### 3. 嵌入式 C 引擎对齐 (C Engine Parity)
* **现状差距**：`c_engine/xirang.c` 作为 34KB 的 ANSI C 实现，仅具备基础的 LLM 单步循环与 Shell 执行，缺少错误指纹匹配、安全过滤与 DoD 网关校验。
* **改进方向**：在纯 ANSI C/C99 零依赖的前提下，增强 C 引擎的 DoD 校验、错误模式匹配与网络容错。

### 4. 大模型调度与容错 (LLM Provider & Orchestration)
* **现状差距**：目前仅支持 OpenAI API 协议格式的单次 HTTP 调用，在网络抖动或模型输出不符合 JSON 规约时缺乏指数退避重试（Exponential Backoff）与多 Provider 自动切换（Failover）。
* **改进方向**：增加多 API Key 轮询、多模型通道自动降级重试机制，并增加输出 JSON 修复校验。

### 5. 自动化测试与 CI/CD 质量防护 (Test & Engineering Quality)
* **现状差距**：测试用例主要覆盖基础安全过滤，对并发批处理、路径穿越防御、技能指纹高命中率匹配及回滚栈等核心能力的单元测试不够充分。
* **改进方向**：全面补齐针对 PathGuard、Rollback 创建文件跟踪、Fast-Path 评分匹配等关键逻辑的单元测试。

---

## 三、 分期演进路线图 (Evolution Roadmap)

```
+-------------------------------------------------------------------------------+
| PHASE 1: 短期打磨 (已在此版本落地)                                             |
| - Fast-Path 多关键词权重与正则复合指纹匹配                                       |
| - PathGuard 绝对路径规范化 (防御路径穿越)                                       |
| - Rollback 事务扩展 (同时追踪 修改文件 与 新建文件)                               |
| - C 引擎错误模式识别与安全防御增强                                             |
| - 完整单元测试覆盖 (go test 100% 通过)                                         |
+-------------------------------------------------------------------------------+
                                       |
                                       v
+-------------------------------------------------------------------------------+
| PHASE 2: 中期深化 (进行中)                                                     |
| - 多 LLM Provider 故障转移与指数退避重试                                        |
| - 智能体结构化输出 JSON Schema 校验与自动修复                                    |
| - XiRang Studio GUI 实时心跳监控面板与技能资产可视化编辑器                       |
| - C 引擎原生 JSON 解析器强化 (不依赖 cJSON 库)                                  |
+-------------------------------------------------------------------------------+
                                       |
                                       v
+-------------------------------------------------------------------------------+
| PHASE 3: 长期远景 (未来规划)                                                   |
| - 基于 TF-IDF / 嵌入式轻量向量的离线技能检索 (0-Token 智能匹配)                   |
| - 操作系统内核级沙箱隔离 (Linux unshare/namespaces, Win Job Objects)           |
| - 边缘多节点协同自愈网络 (Multi-Node XiRang Peer Network)                        |
+-------------------------------------------------------------------------------+
```

---

## 四、 本次落地代码改进总结 (Implemented Improvements)

在此版本的迭代中，我们针对上述差距落地了以下核心改进：

1. **Fast-Path 多关键词评分与正则匹配**：
   - 重构 `findFastSkill`，计算匹配得高分规则，优先选择最匹配的经验脚本，大幅提升 0-Token 秒级自愈准确度。
2. **PathGuard 绝对路径规范化**：
   - 采用 `filepath.Abs`、`filepath.Clean` 和 `filepath.Rel` 严格校验，彻底抵御 `../` 等路径穿越绕过手段。
3. **Rollback 原子撤销栈升级**：
   - 在 `.xirang/backups/` 记录中新增 `created` 字段与类型标识。执行 `--rollback` 时，被修改文件还原，新建文件自动清理。
4. **C 引擎对齐增强**：
   - 增强 `c_engine/xirang.c`，引入错误指纹识别与安全防御提示。
5. **单元测试集拓展**：
   - 在 `main_test.go` 中新增路径穿越攻击防御测试、创建文件回滚测试、Fast-Path 模糊指纹匹配测试等。

---
*文档版本：v4.0-Roadmap | 更新日期：最新 | 归属项目：XiRang Core Engineering Team*
